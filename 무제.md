```dataviewjs
// 1. 렌더링용 내부 모듈 및 컴포넌트 활성화
const { MarkdownRenderer, Component } = require("obsidian");
const comp = new Component();
comp.load();

// 2. 날짜 및 주/월 자동 계산
const todayStr = moment().format("YYYY.MM.DD");
const year = moment().format("YYYY");
const week = moment().format("W[주차]");
const month = moment().format("M[월]");

// 3. 오늘 날짜로 시작하는 일기장 유연하게 찾기
const dailyPage = dv.pages().find(p => p.file.name.startsWith(todayStr));
const dailyFileName = dailyPage ? dailyPage.file.name : `${todayStr} 일기`;

// 4. 3:7 비율 그리드 생성 (간격을 넓혀서 더 깔끔하게 배치)
const grid = this.container.createEl("div", {
    attr: { style: "display: grid; grid-template-columns: 3fr 7fr; gap: 40px; align-items: start;" }
});

const leftCol = grid.createEl("div");
const rightCol = grid.createEl("div");

// 5. 좌측 30% 영역 (바로가기 + iframe 위젯 공간)
// 콜아웃(> [!]) 문법을 빼고 우측과 똑같은 헤더(###) 스타일 적용
const leftContent = [
    "### 📌 바로가기",
    "- [[2028 입시 계획]]",
    "- [[2028 커리큘럼]]",
    "- [[수학 노트]]",
    "- [[카메라 스크랩]]",
    "",
    "<br><br>",
    "### progress",
    "<!-- 👇 아래 줄에 복사해 둔 iframe 코드를 붙여넣으세요 👇 -->",
    "",
    
    <iframe src="https://indify.co/widgets/live/progressBar/e68wM1pKhHVkIxx3Y4qF" width="30%" height="400" style="border: 0;"></iframe>


    "<!-- 👆 위에 코드를 붙여넣으세요 👆 -->"
].join("\n");

// 6. 우측 70% 영역 (오늘 + 이번 주 + 이번 달)
const rightContent = [
    "### today",
    `![[${dailyFileName}#🎯 오늘의 목표]]`,
    "",
    "---",
    "",
    "### this week",
    `![[${year} 주간 목표#${week}]]`,
    "",
    "---",
    "",
    "### this month",
    `![[${year} 월간 목표#${month}]]`
].join("\n");

// 7. 화면에 출력
await MarkdownRenderer.render(app, leftContent, leftCol, dv.current().file.path, comp);
await MarkdownRenderer.render(app, rightContent, rightCol, dv.current().file.path, comp);
```