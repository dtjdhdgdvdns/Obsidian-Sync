---
cssclasses: dashboard-wide
---

# 👋 환영합니다, 내 공간

> "오늘도 목표를 향해 한 걸음씩 나아가는 중!"

---
```dataviewjs
// 1. 옵시디언 내부 렌더러 모듈 불러오기
const { MarkdownRenderer } = require("obsidian");

// 2. 3:7 비율의 그리드 컨테이너 생성
const grid = this.container.createEl("div", {
    attr: { style: "display: grid; grid-template-columns: 4fr 6fr; gap: 16px; align-items: start;" }
});

const leftCol = grid.createEl("div");
const rightCol = grid.createEl("div");

// 3. 왼쪽(바로가기)과 오른쪽(할 일) 마크다운 내용 정의
const leftContent = [
    "> [!bookmark] 📌 바로가기",
    "> - [[2028 입시 계획]]",
    "> - [[2028 커리큘럼]]",
    "> - [[수학 노트]]",
].join("\n");

const rightContent = [
    "> [!check] 📝 오늘 할 일",
    "> - [ ] 수학 기출문제 3개 풀이",
    "> - [ ] 마크 중세 성벽 라인 잡기",
    "> - [ ] 취침 전 일기 기록"
].join("\n");

// 4. 화면에 3:7로 마크다운 렌더링
await MarkdownRenderer.render(app, leftContent, leftCol, dv.current().file.path, this);
await MarkdownRenderer.render(app, rightContent, rightCol, dv.current().file.path, this);
```


# 📊 통합 기록 대시보드
```dataviewjs

window.currentTrackerType =
    window.currentTrackerType || "daily";

const topBar = document.createElement("div");

topBar.innerHTML = `
<label style="font-weight:bold;margin-right:10px;">
🔍 보기 선택:
</label>

<select id="tracker-select"
style="padding:5px 10px;border-radius:5px;">
    <option value="daily"
        ${window.currentTrackerType === "daily" ? "selected" : ""}>
        📝 일상 기록
    </option>

    <option value="dream"
        ${window.currentTrackerType === "dream" ? "selected" : ""}>
        💭 꿈 기록
    </option>
</select>
`;

this.container.appendChild(topBar);

const trackerContainer =
    document.createElement("div");

trackerContainer.style.marginTop = "15px";

this.container.appendChild(trackerContainer);

async function renderTracker() {

    trackerContainer.innerHTML = "";

    let trackerData = {
        entries: []
    };

    if (window.currentTrackerType === "daily") {

        trackerData.heatmapTitle =
            "📝 일상 기록";

        for (let page of dv.pages("#daily")) {

            let dateVal =
                page.file.frontmatter?.date ||
                page.date;

            if (dateVal) {

                trackerData.entries.push({
                    date: window
                        .moment(dateVal)
                        .format("YYYY-MM-DD"),

                    intensity: 1,

                    filePath:
                        page.file.path
                });
            }
        }

    } else {

        trackerData.heatmapTitle =
            "💭 꿈 기록";

        for (let page of dv.pages("#dream")) {

            let dateVal =
                page.file.frontmatter?.date ||
                page.date;

            if (dateVal) {

                trackerData.entries.push({
                    date: window
                        .moment(dateVal)
                        .format("YYYY-MM-DD"),

                    intensity: 1,

                    filePath:
                        page.file.path
                });
            }
        }
    }

    renderHeatmapTracker(
        trackerContainer,
        trackerData
    );
}

await renderTracker();

topBar
.querySelector("#tracker-select")
.addEventListener(
    "change",
    async (e) => {

        window.currentTrackerType =
            e.target.value;

        await renderTracker();
    }
);

```


<iframe src="https://indify.co/widgets/live/progressBar/e68wM1pKhHVkIxx3Y4qF" width="30%" height="400" style="border: 0;"></iframe>



```dataviewjs
// 1. 렌더링용 내부 모듈 불러오기
const { MarkdownRenderer, Component } = require("obsidian");
const comp = new Component();
comp.load();

// 2. 날짜 및 주/월 계산
const todayStr = moment().format("YYYY.MM.DD");
const year = moment().format("YYYY");
const week = moment().format("W[주차]");
const month = moment().format("M[월]");

// 3. 파일 이름이 'YYYY.MM.DD'로 시작하는 노트 찾기 (뒤에 뭐가 붙어도 OK)
const dailyPage = dv.pages().find(p => p.file.name.startsWith(todayStr));
// 파일이 이미 있으면 그 정확한 이름을 쓰고, 아직 없으면 임시로 'YYYY.MM.DD 일기'를 지정
const dailyFileName = dailyPage ? dailyPage.file.name : `${todayStr} 일기`;

// 4. 노션 스타일 1단 레이아웃 구성
const content = [
    "### today",
    `![[${dailyFileName}#🎯 오늘의 목표]]`,
    "",
    "---",
    "",
    "### this week",
    `![[${year} 주간 목표#${week}]]`,
    "",
    "---",
    "",
    "### this month",
    `![[${year} 월간 목표#${month}]]`
].join("\n");

// 5. 화면에 출력
await MarkdownRenderer.render(app, content, this.container, dv.current().file.path, comp);
```




